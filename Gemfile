# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# gem "rails"
gem 'erb_lint', require: false # 追記 ERB Lintを入れる
gem 'pg' # 追記
gem 'sinatra' # 追記
gem 'sinatra-contrib' # 追記
gem 'webrick' # 追記
